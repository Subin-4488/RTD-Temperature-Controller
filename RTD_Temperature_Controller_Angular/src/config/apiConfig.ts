export const API_ENDPOINTS = {
    createConnection: '/connection',
    disconnectConnection: '/connection/disconnect/',
    setMode: '/connection/setatm/',
    getportNames: '/connection/ports/',

    manualMode: '/manualmode',

    settings: '/settings',
  };